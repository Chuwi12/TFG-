[project]
name = "chatbot-backend"
version = "0.1.0"
description = "Backend con IA para el TFG"
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "torch",
    "fastapi",
    "uvicorn",
    "datasets>=3.1.0",
    "transformers>=4.30.0",
]
